package io.pillopl.cinema.reservation;

class ReserveSeatsService {

    ReservationResult reserve() {
        //load seats from db (with versions)
        //rehydreate Hall (maybe Show)
        //load rules
        //reservation.reserve(seats, rules)
        //if success to zapisz i sprawdz locki
        return null;
    }
}
